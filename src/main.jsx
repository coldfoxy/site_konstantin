// ============================================================================
//  main.jsx — ТОЧКА ВХОДА. С этого файла React запускает весь сайт.
//  Трогать обычно не нужно.
// ============================================================================
import { StrictMode } from "react";          // Режим строгой проверки (помогает ловить ошибки)
import { createRoot } from "react-dom/client"; // Функция, которая «вставляет» React в страницу
import "./index.css";                          // Подключаем стили (дизайн-систему)
import App from "./App.jsx";                   // Главный компонент — он собирает все секции

// Находим <div id="root"> в index.html и рисуем внутри него наш сайт (App):
createRoot(document.getElementById("root")).render(
  <StrictMode>
    <App />
  </StrictMode>
);
