# Зависимости (ставятся командой npm install — в репозиторий не нужны)
node_modules

# Собранный сайт (создаётся командой npm run build)
dist

# Системные / редакторские / временные файлы
.DS_Store
*.log
*.local
.vite

# Папка с «сырыми» фото и видео (HEIC/MP4) — тяжёлая, для сайта не нужна.
# Файлы остаются у вас на компьютере, просто не загружаются на GitHub.
# Хотите загрузить и их тоже — удалите строку ниже.
photo
